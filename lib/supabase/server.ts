// Supabase server client completely disabled to prevent btoa errors
// Game works with localStorage only

export async function createClient() {
  // Always return null - Supabase is disabled
  return null
}
